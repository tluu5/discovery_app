<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Discover the best outdoor locations and activities near you.">
    
    <title>Outdoor Activity Discovery</title>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
  </head>

  <body>
    <header>
      <!-- Fixed Navigation Bar -->
      <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
        <div class="container-fluid">
          <%= link_to "Outdoor Activity Discovery", root_path, class: "navbar-brand" %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
              <!-- Home Link (always visible) -->
              <li class="nav-item">
                <%= link_to "Home", root_path, class: "nav-link" %>
              </li>
              <% if user_signed_in? %>
                <!-- Favorites Link (visible only when logged in) -->
                <li class="nav-item">
                  <%= link_to "Favorites", favorites_path, class: "nav-link" %>
                </li>
                <!-- Logout Link (visible only when logged in) -->
                <li class="nav-item">
                  <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link" %>
                </li>
                <% if current_user&.admin? %>
                  <!-- Admin Links (visible only for admin users) -->
                  <li class="nav-item">
                    <%= link_to "Admin Locations", admin_locations_path, class: "nav-link" %>
                  </li>
                  <li class="nav-item">
                    <%= link_to "Admin Users", admin_users_path, class: "nav-link" %>
                  </li>
                <% end %>
              <% else %>
                <!-- Login Link (visible only when logged out) -->
                <li class="nav-item">
                  <%= link_to "Login", new_user_session_path, class: "nav-link" %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <!-- Main Content -->
    <main class="container mt-5">
      <%= yield %>
    </main>

  </body>
</html>
