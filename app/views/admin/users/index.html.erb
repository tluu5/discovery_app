<h1>Admin: Users</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.email %> - <%= user.admin? ? "Admin" : "Regular User" %>
      <%= link_to "Delete", admin_user_path(user), method: :delete, data: { confirm: "Are you sure?" } %>
    </li>
  <% end %>
</ul>
